
from setuptools import setup, find_packages

setup(
    name='public',
    version='0.0.1',
    description="OMERO app for public use to search for images",
    packages=find_packages(),
    keywords=['omero', 'search', 'public'],
)
